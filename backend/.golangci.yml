linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple

linters-settings:
  errcheck:
    ignore: fmt:.*,io/fs:PathError

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
  max-issues-per-linter: 50
  max-same-issues: 10
