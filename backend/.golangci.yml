linters:
  enable:
    - govet
    - staticcheck
    - unused
    - gosimple
  disable:
    - errcheck

issues:
  exclude-rules:
    - path: _test\.go
  max-issues-per-linter: 50
  max-same-issues: 10
